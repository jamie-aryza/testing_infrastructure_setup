; =============================================================================
; SQL Server 2019 Developer - "live" role (OLTP / primary)
; =============================================================================
; Used by: dev-live and test-live EC2 instances in AWS.
;          Mirrors the on-prem prod "live" instance.
;
; Run via: Install-SqlServer.ps1 (which calls setup.exe with this file)
;
; Two-stage install:
;   1. THIS FILE drives the install (features, instance name, paths, collation,
;      auth mode, tempdb layout, service startup types).
;   2. After install, scripts/inventory/Apply-SqlBaseline.ps1 applies the JSON
;      baseline captured from prod (sp_configure, trace flags, DB Mail mode).
;
; Database Mail safety:
;   - SQL Server installs with `Database Mail XPs` = 0 (disabled) by default.
;     A fresh install of this .ini cannot send email even if jobs try.
;   - Apply-SqlBaseline.ps1 defaults to -DbMailMode Disable to keep it off.
;     RedirectToLocal points at smtp4dev/MailHog for safe testing; Match is
;     intentionally not implemented to prevent accidental real email.
;
; OS target: Windows Server 2022.
;
; PoC trade-offs (revisit when scaling beyond t3.micro):
;   - All paths on C: (single EBS volume); prod has separate D:/L:/T: drives.
;   - SQLMAXMEMORY = 768 MB (1 GB t3.micro - leaves OS ~256 MB).
;   - SQLMAXDOP = 1 (single effective vCPU).
;
; Source baseline (Collation, LoginMode, FILESTREAMLEVEL):
;   infrastructure-baseline/local-live (current stand-in for prod).
;   Re-run Inventory-SqlServer.ps1 against real prod and update here.
; =============================================================================

[OPTIONS]

; ---------- Required for unattended install ----------
ACTION="Install"
IACCEPTSQLSERVERLICENSETERMS="True"
IACCEPTPYTHONLICENSETERMS="False"
IACCEPTROPENLICENSETERMS="False"
SUPPRESSPRIVACYSTATEMENTNOTICE="True"
SUPPRESSPAIDEDITIONNOTICE="True"
ENU="True"
QUIET="True"
QUIETSIMPLE="False"
UpdateEnabled="False"
USEMICROSOFTUPDATE="False"
ERRORREPORTING="False"

; ---------- Features ----------
; Match prod's installed feature set. Add ,REPLICATION,FULLTEXT,IS,RS,AS as
; the prod inventory confirms.
FEATURES=SQLENGINE

; ---------- Instance identity ----------
; SQL 2019 = MSSQL15 directory naming.
INSTANCENAME="MSSQLSERVER"
INSTANCEID="MSSQLSERVER"
INSTANCEDIR="C:\Program Files\Microsoft SQL Server"
INSTALLSHAREDDIR="C:\Program Files\Microsoft SQL Server"
INSTALLSHAREDWOWDIR="C:\Program Files (x86)\Microsoft SQL Server"

; ---------- Service accounts (virtual accounts - no password) ----------
SQLSVCACCOUNT="NT Service\MSSQLSERVER"
SQLSVCSTARTUPTYPE="Automatic"
SQLSVCINSTANTFILEINIT="True"

AGTSVCACCOUNT="NT Service\SQLSERVERAGENT"
AGTSVCSTARTUPTYPE="Automatic"

SQLTELSVCACCT="NT Service\SQLTELEMETRY"
SQLTELSVCSTARTUPTYPE="Automatic"

; ---------- Authentication & sysadmins ----------
; No SECURITYMODE = Windows Authentication only (matches prod LoginMode = 1).
; Install-SqlServer.ps1 appends env-specific accounts/SIDs to this line at
; runtime (e.g., the local Administrator and the EC2's SSM-managed admin).
SQLSYSADMINACCOUNTS="BUILTIN\Administrators"

; ---------- Collation (from baseline) ----------
SQLCOLLATION="Latin1_General_CI_AS"

; ---------- Network ----------
; TCP on; Named Pipes off; Browser disabled (using static port 1433).
TCPENABLED="1"
NPENABLED="0"
BROWSERSVCSTARTUPTYPE="Disabled"

; ---------- Database file locations ----------
; PoC: all on C:. Override here once EBS data/log/tempdb volumes are split.
INSTALLSQLDATADIR="C:\Program Files\Microsoft SQL Server"
SQLBACKUPDIR="C:\Program Files\Microsoft SQL Server\MSSQL15.MSSQLSERVER\MSSQL\Backup"
SQLUSERDBDIR="C:\Program Files\Microsoft SQL Server\MSSQL15.MSSQLSERVER\MSSQL\DATA"
SQLUSERDBLOGDIR="C:\Program Files\Microsoft SQL Server\MSSQL15.MSSQLSERVER\MSSQL\DATA"

; ---------- TempDB (best-practice multi-file even on small box) ----------
SQLTEMPDBDIR="C:\Program Files\Microsoft SQL Server\MSSQL15.MSSQLSERVER\MSSQL\DATA"
SQLTEMPDBLOGDIR="C:\Program Files\Microsoft SQL Server\MSSQL15.MSSQLSERVER\MSSQL\DATA"
SQLTEMPDBFILECOUNT="4"
SQLTEMPDBFILESIZE="8"
SQLTEMPDBFILEGROWTH="64"
SQLTEMPDBLOGFILESIZE="8"
SQLTEMPDBLOGFILEGROWTH="64"

; ---------- Other ----------
; Enable FILESTREAM only if prod uses it (FILESTREAMLEVEL > 0 in prod baseline).
FILESTREAMLEVEL="0"
